\* (3) ESCAPE -- the refinement failure is BEHAVIORAL, not just initial.
\* Start inside the formal basin (so the abstract init obligation holds);
\* move under the full extended-basin discipline. TLC returns a
\* boundary-crossing step -- substrate drops below Dissolution while residue
\* keeps ExtWeight >= Formation -- violating the abstract action obligation.
\* The simulation breaks exactly when the extension is exercised.
\* expect: violation
\* tlc-args: -deadlock
SPECIFICATION SpecMaintEscape

CONSTANTS
    L = 4
    Formation = 3
    Dissolution = 2

PROPERTY AbsSpecMaint
