\* (2) Reinvention needs the surviving formal shell HOLDS: the org can only
\* reinvent within a formal shell that survived (informal-collapse death), never
\* after formal dissolution -- IBM-under-Gerstner. Run with -deadlock.
SPECIFICATION Spec

CONSTANTS
    L = 3
    Theta = 2

INVARIANT TypeOK
INVARIANT ReinventionNeedsShell
